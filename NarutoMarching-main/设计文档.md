# 六边形行军路径规划工具 —— 设计文档

> 对应源码：`hex_pathfinding_demo.py`（单文件桌面应用，PyInstaller 打包为 `hex_pathfinding_demo.exe`）
> 文档目的：完整、准确地记录当前软件的**全部功能与机制**，作为后续修改代码后**核对功能是否被破坏**的基准参考。文档中出现的所有数字、公式、按钮位置均来自源码本身，而非概述性描述。
> 生成时间基准：本文档对应的代码版本已修复"地图视图切换保留完整截图"和"点击后台悬浮预览线导致界面永久卡死"两个问题。

---

## 1. 软件是什么

一个 Tkinter/Matplotlib 桌面小工具，帮 3 支行军队伍在一张 60×60 的六边形地图上规划 90 天的每日路线，自动核算：

- 每一步的**粮食消耗**（地形+移动+重复经过折扣等）
- 每支队伍的**每日步数余量**（可跨天结转）
- 每一步获得的**积分（奖励）**
- 特殊地块带来的增益（免费步数、粮食折扣、奖励加成、飞雷神次数）

最终可以把整个 90 天的操作流导出到 Excel 模板，供实际游戏中照抄操作。

---

## 2. 运行依赖 / 文件清单

程序启动时会在 exe（或脚本）同目录下查找以下文件，找不到会直接影响对应功能：

| 文件 | 用途 | 缺失后果 |
|---|---|---|
| `map_S24.csv` | 60×60 地图地块类型表（每格一个 token，如 `1`、`G`、`B1`、`P1`、`ST`） | 程序无法启动（模块级 `_load_csv` 直接读取，无容错） |
| `landInfo.json` | 地块类型 → 颜色/花纹/粮食/步数/奖励的映射表 | 同上，模块级读取，无容错 |
| `S24_分表2.0_导出模板.xlsx` 或 `S24_分表2.0.xlsx`（二选一，按此顺序优先） | Excel 导出的目标模板（1~90 号 sheet 对应每天） | 点击"导出表"提示"template not found"，不影响其余功能 |
| `S24_map_rectified.png` + `S24_map_rectified_extent.json` | "切换地图"用的预处理实景截图及其在内部坐标系中的放置范围 | 点击"切换地图"提示"Map image not found"，不影响六边形视图 |
| `save/autosave/auto_save_day{N}.json` | 自动存档目录（程序自己创建） | 首次运行会自动 `makedirs` |

`S24_map_rectified.png`/`.json` 由离线脚本 `tools/rectify_map_image.py` 从原始截图 `S24_map.png` 生成，运行期不会重新计算，只是加载现成文件（见第 12 节）。

坐标换算约定：地图第 0 行对应游戏内最大行号（`RAW_MAP` 读入后整体 `.reverse()`），即"内部坐标 (ir, ic) 的第 0 行 = CSV 文件最后一行 = 游戏里的第 1 行"。

---

## 3. 六边形网格坐标系

- 网格规模：`ROWS = COLS = 60`（由 `map_S24.csv` 的行列数决定）。
- 单个六边形："flat-top"（平顶）朝向，半径 `HEX_SIZE = 5`。
- 显示阶段额外做了一次"斜视图"拉伸：`X_SCALE = 1`，`Y_SCALE = 0.6`（所有绘制用坐标最终都要乘这两个系数，寻路/成本计算用的是未拉伸的原始 `(ir, ic)` 坐标，两者不要混用）。
- 像素中心公式（`_center(ir, ic)`，未乘 X_SCALE/Y_SCALE）：
  ```
  x = ic * 1.5 * HEX_SIZE
  y = ir * sqrt(3) * HEX_SIZE + (sqrt(3)/2 * HEX_SIZE，若 ic 为奇数)
  ```
- 六邻居方向随列的奇偶性不同（奇数列偏移布局，见 `_neighbors()` 注释里两组方向向量）。
- 距离启发式：先转轴向坐标（`_to_axial`），再用标准六边形曼哈顿距离公式，供 A* 使用（admissible heuristic）。
- 起点：地图中标记为 `"ST"` 的格子；若找不到 `ST`，退回到 `_GAME_START_IR/_GAME_START_IC`（游戏坐标 行53/列8 换算来的内部坐标）。若该格本身是空地（不可通行），用 BFS 就近找一个可通行格子（`_find_passable_near`）。

---

## 4. 地块类型总表（`landInfo.json`）

每个 CSV token 对应一条记录：`name`（内部名）、`label`（显示短名）、`edge`/`face`/`hatch`（描边色/填充色/花纹）、`food`（进入成本，**负数=倒贴给队伍**）、`step`（占用步数，**负数=倒还给队伍步数**）、`award`（获得的积分）。

| Token | 名称 | 粮食 | 步数 | 积分 | 备注 |
|---|---|---:|---:|---:|---|
| `0` | empty（空地） | 0 | 0 | 0 | 不可通行 |
| `1`~`6` | land_1~6 | 100~150 | 1 | 35~60 | 普通地块，数字越大成本/奖励越高 |
| `M` | Merchant（商人） | 0 | 1 | 30 | 免费但占 1 步 |
| `T` | Tent（营地） | **-300**（随天数变化为 -250/-200/-150/-100，天数与数值均在 JSON 里配置，见下） | **-1**（倒还 1 步） | 0 | 唯一会随时间"退化"的地块 |
| `g` | smallGua（小卦） | 120 | 1 | 45 | |
| `G` | bigGua（大卦） | 200 | 1 | 300 | 与 `g` 共同构成"G/g 地"，全部占领后触发全局 20% 粮食减免（见 §6.7） |
| `b` | midBoss（中boss） | 200 | 1 | 500 | |
| `B` | bigBoss（大boss） | 200 | 1 | 1000 | 首次占领时全局飞雷神次数 +1（见 §7.6） |
| `L` | liLian（历练） | 0 | 0 | 520 | 零成本零步数，纯奖励地块 |
| `X1`/`X2`/`X3` | xunji_1/2/3（迅） | 100 | 1 | 35/40/45 | 触发免步数增益（见 §7.5） |
| `B1`/`B2`/`B3` | bingliang_1/2/3（粮） | 100 | 1 | 35/40/45 | 触发粮食折扣增益 |
| `Z1`/`Z2`/`Z3` | zhanxun_1/2/3（勋） | 100 | 1 | 35/40/45 | 触发奖励加成增益 |
| `T2`~`T6` | tower_2~6（塔/草） | 100 | 1 | 40~60 | Excel 导出时归类为"草N" |
| `P1`~`P8` | port（传送门） | 0 | 0 | 0 | 成对出现，触发传送确认弹窗（见 §7.7） |
| `ST` | start（出生点） | 0 | 0 | 0 | |
| `default`（CSV 里未登记的 token） | def | 0 | 0 | 0 | |

**Excel 导出标签映射**（`_operation_label_from_token`）：`1~6`→"N级"，`M`→"商人"，`T`→"营地"，`L`→"历练"，`G`→"大卦"，`g`→"3级卦"，`B`→"大boss"，`b`→"中boss"，`T2~T6`/`B1~3`/`X1~3`/`Z1~3` 统一按数字映射为"草N"。

---

## 5. 队伍（Team）机制

- 最多 3 支队伍：`team1` 在程序启动时自动创建于 `ST` 格；`team2`/`team3` 默认是 `None`，**首次点击 T2/T3 按钮（或按数字键 2/3）时才创建**，创建位置=当前激活队伍的当前所在格，创建日=当前查看的天数。
- 每支队伍独立维护：
  - `full_path`：完整走过的格子序列（含起点）。
  - 一组 `_seg_*` 并行数组，每次点击移动追加一条"段（segment）"记录：`_seg_lengths`（新增格子数）、`_seg_foods`（本段总粮耗）、`_seg_awards`（本段总积分）、`_seg_steps`（本段耗步数）、`_seg_days`（本段归属的天数）、`_seg_new_hexes`/`_seg_jumps`/`_seg_exploration_hexes`（新占领/重复经过/免费探索的格子分类）、`_seg_action_sequence`（该段内每一步的动作类型，供 UI 图标和 Excel 导出使用）、`_seg_is_fly_skill`、`_seg_fly_skill_deltas`（本段对全局飞雷神次数的净影响）、`_no_draw_edges`（传送门/飞雷神跳跃产生的、地图上不画连线的"边"）。
  - `steps`：队伍步数余额（历史字段，实际显示/校验统一改走 `day_records`，见 §8）。
  - `x_bonus_remaining`/`b_discount_remaining`/`z_bonus_remaining` 及对应 `_name`：三种增益的剩余可用次数与来源标记。
  - `max_day_reached`：该队伍走到过的最大天数（用于自动切页、地块归属判定）。
  - `free_exploration_hexes`：已"免费探索"但尚未正式离开（未结算成本）的格子集合。
- **食物是 3 支队伍共用的一个全局池子**（`self.current_food`），**步数是每支队伍独立的池子**（通过 `day_records` 里的 `teamN_steps_remain` 体现）。这是理解"为什么某队看起来卡住"最重要的一条设计前提。

---

## 6. 移动与寻路

### 6.1 点击移动的基本流程
1. 左键点击地图上的一个可通行格子。
2. 用 A*（`_astar`，代价函数 `_cost = max(1, terrain.food)`，启发函数为六边形距离）从当前队伍所在格搜索到目标格的最短路径（`extra_walls` 目前恒为空集，即不排除任何格子）。
3. 若无法到达或路径长度 < 2（原地点击自己）：不做任何事。
4. **单次拖拽路径最长 18 格**（含终点，不含起点），超过则拒绝并提示"Path too long"。
5. 逐格计算成本（见 6.2），一次性作为**一个 segment** 记入当前查看的天数 `self.current_day`（不是该队伍历史最大天数 `max_day_reached`——这一点在 2026-07 的两次 bug 修复中被反复确认为唯一正确语义，见 §13）。

### 6.2 逐格成本分类（`_process_path_click` 核心逻辑）
对路径上每一个新格子 `h`，按以下优先级分类：

1. **重复经过（jump）**：`h` 已被任意队伍占领过，或本段内已经路过一次 → 花费固定 `10` 粮食（受 G/g 减免影响，见 §7.4），0 步数，0 奖励。
2. **零步数地块直接占领**：`terrain.step <= 0`（目前只有 `T`/`L` 这类）→ 花费"移动成本 50（受 G 减免）+ 挑战成本（受 B 折扣、G 减免，Tent 除外不叠加 G 减免）"，获得对应奖励和（负数代表倒还）步数。
3. **免费探索模式**（见 6.3）→ 花费固定 `50` 粮食（受 G 减免），0 步数，0 奖励，格子记入 `exploration_hexes`（尚未正式占领）。
4. **正常新占领**：花费"移动成本 50（受 G 减免）+ 挑战成本（受 B 折扣、G 减免）"，获得地块奖励（受 Z 加成）和地块步数。

**折扣叠加规则**（`_apply_challenge_discounts`）：B 折扣 40% + G 减免 20% 是**相加**而不是相乘，即两者都激活时挑战成本只按 `1 - 0.4 - 0.2 = 0.4` 倍收取；G 减免不作用于 Tent 的挑战成本；折扣只作用于"正成本"，Tent 的负成本（倒贴粮食）不受折扣影响。

### 6.3 免费探索（Free Exploration）机制
- 触发条件：队伍在当前查看天数**步数余额为 0**，且本次只点击了**一个**相邻的、未被任何人占领过的、`step > 0` 的格子，且当前所在格不是"已探索未结算"状态。
- 效果：花 50 粮食（可被 G 减免）"看一眼"这个格子，**不消耗步数、不获得奖励、不算作正式占领**（不加入 `all_visited_hexes`）。
- 之后队伍若要**离开**这个探索格去别处，才会在离开那一刻正式补交这个格子的挑战成本（受 B/G 折扣），并计入 1 步、拿到对应奖励——即"先看后买单"的延迟结算设计。
- 若一直不离开（后续每天继续在此探索格上原地操作），费用不会重复扣。

### 6.4 飞雷神（Fly Skill）
- 全局共享次数 `fly_skill_limit`，初始 `1`，任意队伍首次占领 `bigBoss` 时 `+1`（不限队伍）。
- 点击"飞雷神"按钮进入飞行选择模式（按钮边框变粉色并 300ms 频率闪烁），再点击地图任意一格直接传送过去，**不走 A*，不受"必须相邻/连通"限制**。
- 飞行消耗：
  - 全局飞雷神次数 `-1`（若落地格是**新占领的** `bigBoss`，额外 `+1` 抵消，等于净不消耗）。
  - 粮食：只免除"移动成本 50"这一项（`bigBoss` 除外，`bigBoss` 仍然要付 50 移动成本），挑战成本（受折扣）照常收取；若落地格已被占领过，则按重复经过逻辑收 10 粮食。
  - **不消耗队伍步数**（`seg_steps = 0`），除非落地格 `step` 属性为负数（如 Tent）会倒还步数。
- 飞行降落后同样会触发传送门检测（若落地在传送门格上）、X/B/Z 增益判定，以及 90 天步数结转逻辑。
- 再次点击"飞雷神"按钮可取消飞行模式。

### 6.5 传送门（Portal，P1~P8）
- 每种编号的传送门在地图上恰好一对。
- 队伍**移动路径的终点**落在某个传送门格上时（无论是正常移动还是飞雷神降落），弹出 Tkinter 确认框："是否传送到对面？"
- 选"是"：终点直接改写为配对传送门坐标，起点到该配对点之间的连线标记为"不画"（`_no_draw_edges`），两侧传送门都标记为已占领。
- 选"否"：停留在当前传送门格，仅把当前这一侧标记为已占领。
- 无论选是否，**都算作一次"移动"，会消耗 1 次 B 折扣 / Z 加成剩余次数**（若当前有生效中的增益）。
- 若传送到了一个此前未占领过的目的地，同样会触发该目的地格子的 B1~3/Z1~3 增益判定。

### 6.6 增益地块（B / X / Z）
三种增益都是"落地到**新占领**的对应编号格子"时才会激活，编号 1/2/3 对应的持续次数固定为 **5 / 8 / 10** 次：

| 增益 | 触发地块 | 效果 | 消耗方式 |
|---|---|---|---|
| X（迅） | X1/X2/X3 | 接下来 N 次移动的**步数成本清零**（不含重复经过/探索格） | 仅"新占领"格子消耗一次剩余次数；`_process_path_click` 里会在保存段数据前把命中的格子成本回写为 0 步 |
| B（粮） | B1/B2/B3 | 接下来 N 次移动的**挑战成本打 6 折**（可与 G 减免叠加相加） | 每"新占领"1 格消耗 1 次；传送门交互固定消耗 1 次 |
| Z（勋） | Z1/Z2/Z3 | 接下来 N 次移动的**奖励加 40%**（`round(reward * 1.4)`） | 同 B，按新占领格数或传送门交互消耗 |

三种增益**互不冲突、可同时持有**，各自独立倒计时；地图上是否显示这些地块的文字标签由"显示buff"复选按钮控制（默认开启）。

### 6.7 G/g 全局减免
- 地图上所有 `G`（大卦）和 `g`（小卦）格子的集合是 `all_g_lands`。
- 一旦这些格子**全部**被任意队伍占领过（`visited_g_lands == all_g_lands`），触发全局效果：此后所有"挑战成本"（Tent 除外）、"移动成本 50"、"重复经过成本 10"、"探索成本 50"统一打 8 折（`_apply_g_reduction`，20% 减免）。
- 这个减免是**全局一次性触发、永久生效**的开关，不是消耗次数制。

---

## 7. 天数与资源结转系统

### 7.1 粮食（全队共享池）
- Day 1 起始 `6800` 食物；**从 Day 2 起每天 +1600**（`day_food_available = 6800 + 1600*(day-1)`）。
- `current_food` 是"当前查看天数"这一时间点的粮食余量，切换查看天数时会调用 `_sync_current_food_for_view_day()` 按 `day_records` 里累计已用量重新计算，不是简单地按天数加减。
- 粮食不足时（`current_food < seg_food`）拒绝本次移动，报错信息里会解释"食物是三队共享池"这一事实（`_build_not_enough_food_message`）。

### 7.2 步数（每队独立）
- 每支队伍**每天 +6 步，上限累计 18 步**（可以连续 3 天不动攒到 18，但不会再多）。
- 权威计算在 `_rebuild_day_records()`：对 1~90 天逐日滚动模拟 `team_step_bank = min(bank + 6, 18) - 当天实际耗用`，写入 `day_records[day-1]['teamN_steps_remain']`，这是全软件**唯一权威数据源**（`_get_team_steps_for_day` 优先读它，兜底才用累计公式）。
- 一次移动同时检查粮食和步数两个条件，任一不满足都会拒绝并报错（"Not enough steps! ... Please click Next Day"）。
- **重要设计澄清（2026-07 两次连续 bug 修复的结论）**：一次点击移动，**永远记到"当前查看天数" `self.current_day` 上**，不会因为该队伍"最后一次实际行动的天数"不同而被强制改写。早期版本里存在"点击时自动把查看天数拉回队伍最后行动天"的逻辑，会在以下两种场景下产生错误行为，均已移除：
  1. 队伍某天步数用完后，切到下一天点击仍然被拉回旧天数，导致"明明显示有步数却无法行动"。
  2. 队伍某天只用了部分步数、故意跳到更后面的天数继续走（攒步数打法），新走的步数被错误地记回了旧天数。
  - 现在的唯一保护：**不允许把新动作记到"早于该队伍历史最后行动天"的天数上**（`_is_active_team_locked_by_day`，防止意外改写过去），但允许记到该天或之后任意天。

### 7.3 每周奖励加成
- Day 1 以及此后每逢 `(day - 1) % 7 == 3` 的天（即第 4、11、18、25...天），当天自动 `+500` 积分（`_rebuild_day_records`/导出逻辑里称为"Monday bonus"），与队伍操作无关。

### 7.4 Tent（营地）退化
- Tent 基础粮食 `-300`（倒贴 300 给队伍），随天数推进分 4 档逐步"变差"（对队伍越来越不利）：
  - Day 1~10：-300　Day 11~20：-250　Day 21~35：-200　Day 36~50：-150　Day 51+：-100
- **完全数据驱动，代码里不再硬编码任何天数或金额**：`landInfo.json` 里 `T` 的 `degrade` 字段是一组 `{"day": N, "food": V}`，意思是"从第 N 天起，这个地形的粮食值变成 V"；`_get_terrain_food()` 只是找出"`day <= 当前天`里 `day` 最大的那一条"取其 `food` 值，不满足任何一条时退回该地形的基础 `food`。要调整退化节奏或金额，直接改 `landInfo.json`，不需要碰代码；理论上给任意其它地形加上同样格式的 `degrade` 字段，也会自动生效。

---

## 8. 撤销 / 重置

### 8.1 撤销（撤销按钮 / 无快捷键）
- 只撤销**当前激活队伍**的**最后一条 segment**（与当前查看的天数无关，永远是该队伍历史上最新的一段）。
- 完整回滚：路径点、粮食/积分/步数、飞雷神次数变化、`_no_draw_edges`（传送门/飞行连线标记）。
- **已知简化行为**：撤销会把当前激活队伍的 X/B/Z 三种增益剩余次数**清零**，而不是恢复到撤销前的状态——如果被撤销的那一步本身触发了某个增益，清零是对的；但如果撤销的是"消耗了增益次数的普通移动"，撤销后本该恢复的剩余次数不会被还原，这是有意为之的简化（详见代码注释），验证撤销功能时需注意这一点不是缺陷。
- 撤销后自动保存（`_auto_save_game`）。

### 8.2 重置（点击后二次确认）
- 弹出 Tkinter 确认框，确认后：team1 重新在起点创建，team2/team3 清空为 `None`，全局粮食/积分/天数/飞雷神次数/G地占领记录全部清零回 Day 1 初始状态。视图缩放状态也重置。

---

## 9. 路径编辑（"路径编辑"按钮，也叫 Segment Edit）

用途：修改**某支队伍过去某一天或连续几天**已经走过的路线，而不必用"撤销"一步步倒退到那里。

1. 点击"路径编辑"进入选择模式：自动把查看天数跳到**当前激活队伍**自己的最后行动天（若原本查看的天数该队伍没有任何记录，直接查也找不到东西可编辑）。
2. 在编辑模式下，点击地图上任意一条已画出的路径线段中点，可以**勾选/取消勾选**该段所在的那一天（可以跨天多选，最终取"最小选中天~最大选中天"的连续区间）。
3. 再次点击"路径编辑"按钮：弹出确认框"删除 Day X~Y 的路径并重画？"，确认后：
   - 该队伍在这个天数区间内的所有 segment 被物理删除，路径/资源状态回滚到区间开始前。
   - 若该队伍在区间**之后**还有更晚的历史（future segments），这部分被暂存（不删除，只是先摘下来），并在地图上以预览形式继续显示。
   - 进入"重画"状态：`current_day` 固定为区间起始天，用户重新点击地图画路线。
4. **关键约束**：如果存在"future segments"，编辑模式**不会自动退出**，必须持续在区间起始天重画，直到某一步的落点**恰好回到**被摘下的 future 段原来的起点（`anchor_next_start`），系统才会自动把 future 段重新接回去、退出编辑模式、重新汇总全部队伍的天数分配（`_rebalance_all_teams_from_day`）。如果区间正好是队伍历史的最后几天（没有 future），重画完成后可以直接再点"路径编辑"手动关闭编辑模式。
5. 编辑期间地图点击不再走正常移动逻辑，而是走独立的 `_handle_segment_edit_click`（选段）或"重画中"逻辑；退出前无法进行正常移动/切天操作之外的大部分交互。

---

## 10. 队伍切换与"跳到下一天"

- **T1/T2/T3 按钮 或 数字键 1/2/3**：
  - 若目标队伍尚不存在（T2/T3 首次使用）：立即在当前激活队伍所在位置创建它并切换过去。
  - 若目标队伍已存在且**不是**当前激活队伍：正常切换激活队伍，视图居中到"该队伍在当前查看天数的行动范围"（若当天该队伍没有任何行动，则**保持视图不动**，不会跳去它的历史位置——避免把用户正在看的画面莫名切走）。
  - 若目标队伍已经是当前激活队伍（**再点一次同一个按钮，或 0.4 秒内双击**）：触发"跳到下一天"——把查看天数直接设为"该队伍历史最后行动天 + 1"（`_switch_to_team_and_latest_plus_one_day`），方便"这天步数用完了，直接跳到明天接着走"。
- 双击判定窗口：`0.4` 秒（`_team_button_dblclick_window_sec`）。

---

## 11. 存档 / 读档

### 11.1 手动存档（"保存"按钮）
- 弹出系统"另存为"对话框，把完整游戏状态（两支/三支队伍全部 `_seg_*` 数组、`current_day`、`current_food`、`total_food`、`total_reward`、`fly_skill_limit`、`all_visited_hexes`、`visited_g_lands`、`active_team_num`、`day_records`）写成 JSON。

### 11.2 手动读档（"读取"按钮）
- 弹出系统"打开"对话框，读取上述 JSON 结构，重建 3 支队伍对象。
- 读档后会做一致性自检：
  - `_has_global_food_deficit_from_segments()` 或任一队伍 `_team_has_step_overflow_from()` 为真时，先跑一遍 `_rebalance_all_teams_from_day(1)` 自动修正天数分配（可能重排某些 segment 归属的天数）。
  - 之后统一调用 `_rebuild_shared_derived_state_from_segments()`（重放全部队伍历史，重建 `full_path`/`visited_hexes`/`max_day_reached` 等派生状态）和 `_rebuild_day_records()`（重建权威的每日食物/步数账本），并按当前查看天数重新同步 `current_food`。
- 因此**存档文件里的 `day_records` 字段仅供参考，读档时永远会被重新计算覆盖**，不依赖存档时是否精确。

### 11.3 自动存档
- 几乎每次成功的移动/飞行/撤销/切天操作之后都会静默调用 `_auto_save_game()`。
- 保存路径固定为 `<程序目录>/save/autosave/auto_save_day{N}.json`，**N = 当前三支队伍中"历史最后行动天"的最大值**（不是当前查看天数）。
- 同一个 N 会反复覆盖同名文件；程序本身**不会在启动时自动读取任何自动存档**——必须手动点"读取"选择文件（这一点容易被误解，验证时注意区分"自动存档"和"自动加载"）。

---

## 12. 地图视图切换（"切换地图"按钮）

- 默认是软件自绘的六边形网格视图（`_map_view_mode = 'hex'`）。
- 点击"切换地图"切到 `'image'` 模式：整张地图背景替换成预先离线处理好的实景截图 `S24_map_rectified.png`，队伍路径/标记/图例照常绘制在图片上方（同一坐标系，`imshow(extent=...)` 摆放，`zorder=0` 保证在最底层）。
- 图片**首次**切换时才会从磁盘加载并缓存到 `self._map_image_array`，之后来回切换不会重复读盘。
- 该截图是通过离线脚本 `tools/rectify_map_image.py` 生成的：自动检测截图里每个六边形色块的质心，锚定唯一的"ST"高亮格，最小二乘法拟合出一个"内部 (ir, ic) ↔ 截图像素坐标"的整体仿射变换，然后**保留整张原始截图不做任何裁剪/抠图/透明化**，仅按需缩小分辨率（长边不超过 2600px）后按这个仿射变换摆放到内部坐标系里。放置范围（`extent`）随截图一起存成 `S24_map_rectified_extent.json`，运行时直接读取，不在运行期重新计算。
- "157 He Culture"斜纹水印在两种模式下都会绘制（`zorder=0`），实景图模式下水印会被不透明的截图（同为 `zorder=0` 但先画）盖住，视觉上不可见，但绘制逻辑本身不区分模式。
- 若资源文件缺失，点击按钮会在状态栏提示具体缺哪个文件，并保持在六边形模式。

---

## 13. 界面布局与交互控制

### 13.1 窗口与坐标（`__init__` 里的 figure-relative 坐标，可用于按钮定位/自动化测试）

| 元素 | 位置（figure 坐标，左下为原点，宽 16×高 10 英寸） |
|---|---|
| 主地图区 | `[0.01, 0.05, 0.905, 0.93]` |
| 撤销 | `[0.914, 0.08, 0.043, 0.025]` |
| 重置 | `[0.957, 0.08, 0.043, 0.025]` |
| 飞雷神 | `[0.914, 0.032, 0.043, 0.025]` |
| 显示未来 | `[0.914, 0.056, 0.086, 0.022]` |
| 显示buff（复选框样式） | `[0.957, 0.032, 0.043, 0.025]` |
| 前一天(Q) | `[0.02, 0.02, 0.09, 0.035]` |
| 后一天(E) | `[0.12, 0.02, 0.09, 0.035]` |
| 切换地图 | `[0.22, 0.02, 0.09, 0.035]` |
| 读取 | `[0.914, 0.002, 0.043, 0.025]` |
| 保存 | `[0.957, 0.002, 0.043, 0.025]` |
| 全局统计 | `[0.827, 0.002, 0.084, 0.025]` |
| 路径编辑 | `[0.737, 0.002, 0.084, 0.025]` |
| 导出表 | `[0.647, 0.002, 0.084, 0.025]` |
| T1 / T2 / T3 | `[0.909, 0.735]` / `[0.937, 0.735]` / `[0.965, 0.735]`，宽高均 `0.022×0.025` |
| Day/日期徽章（点击弹出跳天选择框） | `fig.text(0.95, 0.86, ...)` |

窗口标题固定为 `Hex Grid – A* Waypoint Path – 3 Teams  |  redblobgames.com/grids/hexagons/`；默认隐藏 Matplotlib 自带的导航工具栏和坐标状态条（`rcParams['toolbar'] = 'None'`）。

### 13.2 鼠标操作
- 左键点击地图：按当前模式执行"移动/飞行落点/传送门确认/路径编辑选段"之一。
- 悬停 0.5 秒不动：若目标格可达且负担得起，显示一条半透明虚线预览路径（`_on_hover_timeout`）；离开或移到别的格子会立即清除。
- 右键拖动：平移视图（记录拖动起点在"拖动开始那一刻冻结"的坐标变换下的位置，逐帧换算位移量，避免边拖边平移导致参照系漂移）。
- 滚轮：以鼠标当前指向的数据坐标为中心缩放（向上滚=放大 0.8 倍，向下滚=缩小 1.2 倍）。

### 13.3 键盘快捷键（`_on_key_press`）
| 按键 | 行为 |
|---|---|
| `1`/`2`/`3` | 与点击对应 T1/T2/T3 按钮完全相同的逻辑（含双击/双击判定共用同一份 0.4 秒计时状态） |
| `Q` | 前一天 |
| `E` | 后一天 |
| `X` | 导出 Excel（与"导出表"按钮相同） |

### 13.4 视图自动居中/动画
- 切队伍、切天、飞行落地等场景会触发"视图平滑移动到目标位置"（`_animate_view_to`，18 步、90ms 内完成的插值动画）。
- 若用户已经手动缩放过（`_has_zoomed = True`），后续的"自动居中"逻辑会尽量保留用户当前的缩放级别，只平移不重新适配整张地图。
- 当前激活队伍在地图上的标记有"呼吸"闪烁动画（`_breathing_timer`/`_update_blinking`），用于醒目地区分"正在操作的是哪支队伍"。

---

## 14. Excel 导出（"导出表"按钮 / 快捷键 X）

1. 需要 `openpyxl`；找不到会弹窗提示并中止（不影响软件其余功能）。
2. 按运行目录 → 脚本目录 → PyInstaller 临时目录（`_MEIPASS`）的顺序查找模板文件，输出固定命名为 `S24_分表2.0_export.xlsx`，与模板同目录。
3. 导出前会先跑一遍 `_rebuild_day_records()`，保证写出去的是最新状态。
4. 对模板里存在的 `"1"`~`"90"` 号 sheet（sheet 名即天数字符串，不存在的天直接跳过不报错）：
   - 三支队伍的"操作流"分别写在第 8/11/14 行、从第 C 列（第3列）到第 AA 列（第27列）共 25 格操作槽位，每格是 `_operation_label_from_token` 映射出的中文短标签（超过 25 步的操作会被截断，只写前 25 个）。
   - `C4` 写当天 BXZ 增益带来的粮食净调整量，`C6` 写当天 BXZ 增益带来的奖励净调整量再加上"每周 500 奖励"（若当天符合规则）。
   - 第 2/3/4 行第 I 列（第9列）分别写三支队伍**当天结束时**的剩余步数（直接取自 `day_records`）。
5. 成功/失败都会弹出 Tkinter 消息框做二次确认，同时把结果写进 `self._status_msg`。

---

## 15. 统计窗口

### 15.1 全局统计（"全局统计"按钮，弹出独立窗口，标题"全局统计"）
- 表 1：按地块类型（排除传送门、`ST`、B/X/Z 增益格）统计"总数 / 已占领 / 剩余"，按地块颜色/花纹渲染表格底色。
- 表 2："跳步汇总"：按"G/g 地是否已全部占领"这个全局里程碑，把每支队伍的"跳步次数"拆成 触发前/触发后 两段分别统计。计数规则（`_compute_jump_summary_before_after_g` 内的固定规则）：
  - 普通重复经过（jump）记 1 跳。
  - **首次**经过某传送门记 5 跳。
  - 经过一个**已经用过**的传送门记 1 跳。
  - 飞雷神直接飞到传送门上记 **0 跳**。
- 该窗口每次点击"全局统计"都会重新计算并刷新（`_ensure_global_stat_window` 会检测窗口是否已被用户关闭，需要的话自动重建 figure）。

### 15.2 "Game Data" 窗口 —— **当前 UI 中没有任何入口能打开它（遗留/未接线功能）**
- 代码里完整存在（`self.data_fig`/`self.data_ax`/`_draw_data_window`），内容是"当前查看天的食物消耗/剩余、三队当天剩余步数、三队当天占领地块列表"。
- 但通篇搜索确认：`self.data_fig.show()` 从未被调用，`_data_window_open` 也从未被设为 `True`。这是一个**完整实现但已经没有触发入口的死功能**，很可能是被"全局统计"窗口取代后没有清理。核对功能时不要因为"找不到 Game Data 窗口"而误判为 bug——它本来就打不开。

### 15.3 右侧常驻信息（主窗口内，不是弹窗）
- 食物/积分小表格（"余粮"/"总分"，`_draw_map_stats_table`）：显示当前查看天的剩余食物和"截至当前天"的累计积分。
- T1/T2/T3 按钮上方数字（`_update_team_button_labels`）：当前查看天每支队伍的剩余步数（与全局统计窗口无关，实时刷新）。
- 按钮下方竖排彩色方块图标（`_draw_team_action_symbols`）：当前查看天每支队伍依次做过的动作（新占领=对应地块颜色方块，连续多次重复经过合并显示为一个跳跃图标），最多按需扩展到 30 个图标高度。
- 右上角"Day N (M/D)"徽章：点击可弹出 1~90 天列表选择框直接跳转（`_show_day_picker`），日期以 **Day 1 = 2026/6/12** 为基准逐天推算。

---

## 16. 视觉与性能相关的非功能性设计点

以下内容不影响游戏结果，但会影响"看起来是否正常"，核对 UI 变化时需要知道这是有意为之：

- 地图背景无颜色（Matplotlib 默认透明/白色），不做任何额外填色处理。
- 六边形地形从"每格一个 `RegularPolygon` + `add_patch`"重构为"按花纹分组、批量 `PolyCollection` + `add_collection`"，这是纯性能优化（原方案每次重绘约 450~500ms，是当时最大的卡顿来源），不改变任何视觉或游戏逻辑结果。
- 默认隐藏 Matplotlib 自带工具栏/坐标状态条，因为它的平移/缩放按钮会和软件自定义的右键拖动/滚轮缩放冲突。

---

## 17. 已知的历史 Bug 与修复结论（供回归测试对照）

这两条是本文档编写前最近修复的问题，**回归测试时应重点覆盖**：

1. **"某队伍步数明明够用却完全无法行动、翻页按钮正常"**
   根因：`_draw()` 每次重绘都会 `ax.clear()`，销毁当前正在显示的"悬浮预览虚线"这个 Matplotlib artist 对象，但代码没有同步把 `self._hover_path_line` 这个 Python 引用置空。下一次点击/移动鼠标时，`_clear_hover_preview()` 对着这个已失效的对象调用 `.remove()` 触发 `NotImplementedError`，而这个异常发生在把引用置空**之前**，加上 Matplotlib 会吞掉回调函数里的异常（只打印到控制台，不崩溃、不提示），导致**此后所有点击和鼠标移动永久性地在同一处失败**，界面上没有任何提示。翻页按钮（Button 控件的独立回调）不经过这段代码，所以仍然正常。
   验证方法：观察启动 exe 时弹出的黑色控制台窗口，若持续大量打印 `NotImplementedError: cannot remove artist`，说明该 bug 复现。
   修复：`_draw()` 在 `ax.clear()` 之后立即同步重置 `_hover_path_line`/`_hover_timer`/`_hover_hex`；`_clear_hover_preview()` 自身也加了 `try/except NotImplementedError` 兜底，即使未来出现类似的竞态也不会再永久卡死。

2. **"这天用剩的步数被强制记到别的天上，导致新点开的天数无法行动"**
   根因：早期的"点击时自动把查看天数拉回队伍最后行动天"逻辑，无论该天是否已经用完步数、无论用户是否是特意跳到别的天去继续走，都会强制生效，与"新动作应该记到用户当前正在看的那一天"这个基本预期冲突。
   修复：完全移除该自动纠正逻辑，`self.current_day` 只由用户显式操作（前一天/后一天/跳天弹窗/双击队伍按钮）改变，点击移动永远记到当时的 `self.current_day` 上；唯一保留的保护是禁止把新动作记到"早于该队伍历史最后行动天"的天数（防止意外覆盖过去）。

---

## 18. 功能核对速查清单

修改代码后，建议至少手动过一遍以下场景确认没有回归：

- [ ] 新建 team2 / team3（点击尚不存在的 T 按钮），起点是否为当前激活队伍当前位置。
- [ ] 同一天连续移动到步数耗尽，报错文案是否提示"点下一天"。
- [ ] 步数未耗尽时故意跳到后面几天再继续走该队伍，新段落是否记在**跳转后**的那一天，而不是被拉回原来的天。
- [ ] 免费探索：步数为 0 时点相邻未占领格 → 只扣 50 粮食，不掉步数、不加分；之后离开该格才补交挑战成本。
- [ ] 踩到传送门：确认框选"是"传送、选"否"停留，两种情况地图连线是否符合预期（不画出穿地图的直线）。
- [ ] 飞雷神：目标已占领 vs 未占领、目标是 bigBoss、步数/粮食是否符合"不扣步数、只免移动成本"的规则。
- [ ] G/g 地全部占领后，后续所有成本类型是否统一多打 8 折（叠加 B 折扣时是相加不是相乘）。
- [ ] X/B/Z 增益：命中编号 1/2/3 的地块次数是否分别是 5/8/10；重复经过/免费探索格不消耗次数。
- [ ] 撤销：只回退当前激活队伍最后一段，X/B/Z 增益按设计清零（不是恢复）。
- [ ] 路径编辑：删除中间某天后，是否要求重新连回 future 锚点才能自动退出编辑模式。
- [ ] 存档/读档：读档后 `day_records` 是否被重新计算覆盖（而不是照搬存档里的旧值）。
- [ ] 自动存档文件名是否用"三队最后行动天最大值"而不是"当前查看天"。
- [ ] 切换地图：图片是否完整无裁切显示，路径/标记是否仍正确叠加在图片正确位置上。
- [ ] Excel 导出：无模板/无 openpyxl 时是否给出清晰报错而不是崩溃。
- [ ] 黑色控制台窗口在正常操作全流程中不应出现任何未捕获的 Traceback。
